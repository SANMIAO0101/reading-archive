/*
  Production configuration.
  Fill these two values after creating a Supabase project.
  They are safe to expose in the browser when RLS is correctly configured.
*/
window.RA_CONFIG = window.RA_CONFIG || {
  supabaseUrl: '',
  supabaseAnonKey: '',
  demoMode: true
};
